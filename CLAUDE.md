# CLAUDE.md — coding_study 운영 규약

이 저장소는 **코딩 테스트 학습**용이다. 새 세션이 시작될 때 이 문서가 자동으로 읽힌다.
아래 규약을 그대로 따라 문제를 출제·채점·이력관리한다. 사용자에게는 **한국어 합니다체**로 응답한다.

## 역할과 흐름

**Claude가 문제 출제 → 사용자가 풀이 → Claude가 채점 → git 이력 관리** 순으로 진행한다.

- 언어: **Python 3**
- 문제 출처: **프로그래머스(programmers)**, **직접 출제(custom)**
- 원격: https://github.com/Gyobeom/coding_study_test.git (branch `main`)

---

## 1. 문제 출제 (Claude)

사용자가 "문제 내줘" 류로 요청하면 아래 체크리스트대로 **문제 폴더 1개**를 생성한다.

### 1-1. 폴더 경로·이름 규약

```
problems/<출처>/<분류>/<번호>_<문제이름>/
```

- 출처: `programmers` 또는 `custom`
- 분류:
  - programmers → `lv1` / `lv2` / `lv3`
  - custom → 분류 폴더 없이 `problems/custom/<번호>_<이름>/`
- 번호: **출처별로** 3자리 zero-padding, 1부터 증가 (`001`, `002`, …).
  새 번호는 해당 출처 폴더의 기존 최대 번호 + 1. (예: `problems/custom/` 에 001이 있으면 다음은 002)
- 문제이름: 한글 가능, 공백은 `_` 로. (예: `002_짝수와_홀수`)

### 1-2. 생성할 파일 3개

`templates/` 의 원본을 기준으로 아래를 만든다.

**① `README.md`** — 문제 설명. 다음 항목을 채운다:
- 제목(`# 문제이름`)
- 메타: **출처 / 난이도(★☆☆~★★★) / 분류 / 링크(있으면)**
- `## 문제` — 설명
- `## 제약 조건` — 입력 범위·조건 (시간복잡도 판단 근거가 되므로 반드시 명시)
- `## 입출력 예시` — 표로 최소 2개
- `## 함수 시그니처` — `def solution(...)` 형태 명시
- `## 풀이 노트 (풀이 후 작성)` — 접근/시간복잡도/배운 점 (빈 칸으로 둠)

**② `solution.py`** — **빈 템플릿**. `solution(...)` 함수만 두고 본문은 미구현으로 둔다.
사용자가 직접 푸는 자리이므로 **정답을 채우지 않는다.** 시그니처는 문제에 맞게 인자명을 정한다.

**③ `test.py`** — 채점 테스트. 아래 형식을 지킨다. 상단의 sys.path 헤더는 **그대로 복사**한다.

```python
"""채점 테스트 (Claude가 작성). 실행: python test.py"""
import os
import sys

# --- 저장소 루트를 찾아 sys.path에 추가 (수정 불필요) ---
_here = os.path.dirname(os.path.abspath(__file__))
_cur = _here
while not os.path.isfile(os.path.join(_cur, "lib", "judge.py")):
    _parent = os.path.dirname(_cur)
    if _parent == _cur:
        raise RuntimeError("저장소 루트(lib/judge.py)를 찾지 못했습니다.")
    _cur = _parent
sys.path[:0] = [_here, _cur]
# ---------------------------------------------------------

from lib.judge import check
from solution import solution

# 테스트 케이스: (인자_튜플, 기대값). 인자가 하나여도 튜플로: ((5,), 25)
cases = [
    (([2, 7, 11, 15], 9), [0, 1]),
    # 엣지 케이스(빈 입력·경계값·음수·중복 등)를 반드시 포함
]

if __name__ == "__main__":
    check(solution, cases)
```

- 테스트 케이스는 **정상 케이스 + 엣지 케이스(경계값/빈 입력/음수/중복/최대 규모)** 를 골고루 넣는다(최소 5개 권장).
- 출제 시 Claude는 내부적으로 정답을 알고 케이스의 기대값을 계산하되, `solution.py` 에는 정답을 쓰지 않는다.

### 1-3. 출제 직후

- `PROGRESS.md` 이력 표에 **🟡 출제됨** 행을 추가한다(아래 3절 형식).
- git 커밋: `문제: <출처> <이름> 출제`
- 사용자에게 문제 요약과 **풀이할 파일 경로(`solution.py`)** 를 안내한다.

---

## 2. 채점 (Claude)

사용자가 풀이를 끝냈다고 하면:

1. `python problems/<...>/test.py` 를 실행한다. (전체 확인은 `python lib/run_all.py`)
2. 결과를 **구체적 수치로** 보고한다: `N/M 통과`, 실패 케이스는 입력/기대값/실제값을 그대로 전달.
3. **실패 시**: 틀린 케이스를 근거로 힌트를 준다. 사용자가 원하기 전에는 정답 코드를 대신 채우지 않는다.
4. **통과 시**: 필요하면 `README.md` 의 "풀이 노트"에 접근법·시간복잡도·배운 점을 함께 정리한다.
   더 나은 풀이(시간복잡도 개선 등)가 있으면 **제안**한다.
5. `PROGRESS.md` 상태를 갱신(🟢 통과 / 🔴 실패)하고 커밋: `풀이: <출처> <이름> (통과 N/N)`

### 채점기 계약 (`lib/judge.py`)

`check(solution_fn, cases)` — 각 케이스를 실행해 `==` 비교, ✅/❌ 및 실행시간(ms) 출력.
모두 통과 시 종료코드 0, 하나라도 실패/예외 시 1. 반환값 비교이므로 **풀이는 print가 아니라 return** 해야 한다.

---

## 3. 이력 관리 (`PROGRESS.md`)

`## 이력` 표에 행을 추가/갱신한다. 날짜는 **오늘 날짜(절대값)** 로 적는다.

```
| 날짜 | 출처 | 문제 | 난이도 | 상태 | 채점 | 비고 |
```

- 상태 범례: 🟡 출제됨 · 🟠 풀이중 · 🟢 통과 · 🔴 실패(재도전)
- 상단 "요약" 표의 총 출제/풀이 완료/통과 카운트도 함께 갱신한다.

---

## 4. 커밋 컨벤션

- `문제: <출처> <이름> 출제` — 새 문제 추가
- `풀이: <출처> <이름> (통과 N/N)` — 풀이 및 채점 결과
- `학습: <주제>` — 학습 노트 추가
- `구성: <내용>` — 프로젝트 구조/설정 변경

커밋 메시지 말미에 다음을 붙인다:
```
Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

**push(원격 전송)는 자동으로 하지 않는다.** 사용자가 명시적으로 요청할 때만 `git push` 한다.

---

## 5. 난이도·분류 기준

- 난이도: `★☆☆`(쉬움) / `★★☆`(보통) / `★★★`(어려움). 프로그래머스는 lv1≈★☆☆, lv2≈★★☆, lv3≈★★★ 로 매핑.
- 분류 예시: 구현, 문자열, 정렬, 완전탐색, 이분탐색, 그리디, DP, 그래프/BFS·DFS, 자료구조(스택·큐·힙), 수학, 해시.
