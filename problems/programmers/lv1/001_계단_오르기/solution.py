def solution(n):
    """0칸에서 시작해 한 번에 1칸 또는 2칸씩 올라 n칸에 정확히 도달하는
    서로 다른 방법의 수를 반환.

    여기에 풀이를 구현하세요. (지금은 빈 템플릿이라 채점 시 실패합니다.)

    힌트:
      dfs(pos) = "pos에서 n까지 가는 방법의 수" 로 정의하면
        - pos == n 이면 1 (도착)
        - pos >  n 이면 0 (지나침)
        - 그 외    이면 dfs(pos+1) + dfs(pos+2)
      마지막에 dfs(0) 을 반환.
    """
    answer = 0
    return answer
