def solution(rows, columns, queries):
    """여기에 풀이를 구현한다.

    - 인자: rows(행 수), columns(열 수), queries([x1, y1, x2, y2] 배열, 1-indexed).
    - 반환: 각 쿼리에서 회전으로 이동한 값 중 최솟값을 순서대로 담은 배열. 자세한 규칙은 README.md 참고.
    - 반환값이 곧 정답이다. print 가 아니라 return 으로 돌려준다.
    """
    answer = None
    return answer
