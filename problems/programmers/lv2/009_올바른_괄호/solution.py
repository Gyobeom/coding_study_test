def solution(s):
    """여기에 풀이를 구현한다.

    - 함수 이름은 반드시 `solution` 으로 둔다(채점기가 이 이름을 호출한다).
    - 인자: s('(' 와 ')' 로만 이루어진 문자열).
    - 반환값: 올바른 괄호이면 True, 아니면 False 를 return 한다.
    """
    answer = None
    return answer
