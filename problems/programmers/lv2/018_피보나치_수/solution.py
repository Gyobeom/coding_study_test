def solution(n):
    """n번째 피보나치 수 F(n)을 1234567로 나눈 나머지를 반환한다.

    - n: 구할 항의 인덱스 (int).
    - 반환: F(n) % 1234567 (int).
    - print 가 아니라 return 으로 돌려준다.
    """
    answer = None
    return answer
