def solution(begin, target, words):
    """begin에서 target으로 변환하는 최소 단계 수를 반환한다(불가능하면 0).

    - 한 번에 한 글자만 바꿀 수 있고, words에 있는 단어로만 변환 가능하다.
    - 반환값이 곧 정답이다. print가 아니라 return으로 돌려준다.
    - 힌트는 README.md 참고. 여기에 직접 구현한다.
    """
    answer = None
    return answer
