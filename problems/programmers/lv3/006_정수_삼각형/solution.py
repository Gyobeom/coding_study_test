def solution(triangle):
    """맨 위에서 맨 아래까지 내려오는 경로 합의 최댓값을 반환한다.

    - triangle: 리스트의 리스트. triangle[i]의 길이는 i+1.
    - 반환: 경로 합의 최댓값 (int).
    - print 가 아니라 return 으로 돌려준다.
    """
    answer = None
    return answer
