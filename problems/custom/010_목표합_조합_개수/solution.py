def solution(nums, target):
    """여기에 풀이를 구현한다.

    - 함수 이름은 반드시 `solution` 으로 둔다(채점기가 이 이름을 호출한다).
    - nums: 양의 정수 배열, target: 목표 합.
    - 각 인덱스를 최대 한 번 골라 합이 target 이 되는 부분집합의 개수를 return 한다.
      (값이 같아도 서로 다른 인덱스에서 뽑으면 다른 경우로 센다.)
    """
    answer = None
    return answer
