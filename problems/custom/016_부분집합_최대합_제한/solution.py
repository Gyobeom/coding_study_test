def solution(nums, limit):
    """여기에 풀이를 구현한다.

    - 함수 이름은 반드시 `solution` 으로 둔다(채점기가 이 이름을 호출한다).
    - nums: 정수 배열, limit: 정수.
    - 원소 합이 limit 이하가 되는 부분집합 중 최대 합을 return 한다.
    - 반환값이 곧 정답이다. print 가 아니라 return 으로 돌려준다.
    """
    answer = None
    return answer
