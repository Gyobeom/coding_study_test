def solution(grid):
    """여기에 풀이를 구현한다.

    - 함수 이름은 반드시 `solution` 으로 둔다(채점기가 이 이름을 호출한다).
    - grid: 0/1 로 이루어진 2차원 리스트.
    - 각 섬의 넓이를 오름차순 정렬한 리스트를 return 한다.
    """
    answer = None
    return answer
