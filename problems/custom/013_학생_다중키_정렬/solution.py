def solution(students):
    """여기에 풀이를 구현한다.

    - 함수 이름은 반드시 `solution` 으로 둔다(채점기가 이 이름을 호출한다).
    - students: [이름(str), 점수(int)] 원소들의 리스트.
    - 점수 내림차순, 점수 동점이면 이름 오름차순으로 정렬한 뒤
      이름만 순서대로 담은 리스트를 return 한다.
    - 반환값이 곧 정답이다. print 가 아니라 return 으로 돌려준다.
    """
    answer = None
    return answer
