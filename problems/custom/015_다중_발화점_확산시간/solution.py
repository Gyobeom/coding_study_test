def solution(grid):
    """여기에 풀이를 구현한다.

    - 함수 이름은 반드시 `solution` 으로 둔다(채점기가 이 이름을 호출한다).
    - grid: R×C 격자. 0=벽, 1=아직 젖지 않은 칸, 2=발화점(시작점, 여러 개 가능).
    - 모든 1이 젖는 데 걸리는 최소 시간을 return 한다.
      젖을 1이 없으면 0, 도달 불가한 1이 남으면 -1.
    - 반환값이 곧 정답이다. print 가 아니라 return 으로 돌려준다.
    """
    answer = None
    return answer
