def solution(maps):
    """여기에 풀이를 구현한다.

    - 함수 이름은 반드시 `solution` 으로 둔다(채점기가 이 이름을 호출한다).
    - maps: 1(길)과 0(벽)으로 이루어진 n×m 2차원 리스트.
    - (0,0)에서 (n-1,m-1)까지 지나는 칸 수의 최솟값을 return 한다.
      도달할 수 없으면 -1 을 return 한다.
    """
    answer = None
    return answer
