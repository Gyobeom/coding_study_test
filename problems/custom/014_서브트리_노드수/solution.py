def solution(parent):
    """여기에 풀이를 구현한다.

    - 함수 이름은 반드시 `solution` 으로 둔다(채점기가 이 이름을 호출한다).
    - parent: 길이 n의 부모 배열. parent[i]는 노드 i의 부모, 루트는 -1.
    - 각 노드를 루트로 하는 서브트리의 노드 수(자기 포함)를
      노드 번호 순서대로 담은 리스트를 return 한다.
    - 반환값이 곧 정답이다. print 가 아니라 return 으로 돌려준다.
    """
    answer = None
    return answer
